# === Conexão com o banco (PostgreSQL) ===
# Local (Postgres no Docker - docker-compose.dev.yml):
#   jdbc:postgresql://localhost:5432/patudos
# Produção (Railway / Render):
#   jdbc:postgresql://<host>:<port>/<database>
DB_URL=

DB_USER=
DB_PASSWORD=

# === Hibernate (opcionais, com defaults razoáveis) ===
# DDL_AUTO=update      # ou 'validate' em produção
# SHOW_SQL=false
