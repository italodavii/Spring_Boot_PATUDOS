services:
  oracle:
    image: gvenzl/oracle-free:23-slim-faststart
    container_name: patudos-oracle-dev
    environment:
      ORACLE_PASSWORD: senhalocal123
      APP_USER: patudos
      APP_USER_PASSWORD: senhalocal123
    ports:
      - "127.0.0.1:1521:1521"
    volumes:
      - patudos_dev_data:/opt/oracle/oradata

volumes:
  patudos_dev_data: